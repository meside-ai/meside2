import { Box } from "@mantine/core";

export const PreviewPanel = () => {
  return (
    <Box
      display="flex"
      style={{
        flexDirection: "column",
        width: "100%",
        height: "100%",
        overflow: "hidden",
      }}
      py="sm"
    >
      not supported
    </Box>
  );
};
